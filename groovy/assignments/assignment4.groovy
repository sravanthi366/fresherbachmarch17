// create a list of integers add them together and double the list of integer and add them together and compute their average.

def list = [1,2,3,4]
println list.value.sum()
def list1 = list.value*2
println list1

println list1.value.sum()
